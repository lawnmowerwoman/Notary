# Notary Tools

This directory contains public build and maintenance helpers.

Confidential deployment helpers that provision Jamf API credentials, Extension
Attributes, or Ministry transport state are intentionally not part of the public
source tree. Authorized Ministry builds keep those files below `*.local/`
directories, such as `Sources.local/Deployment/`.
